// TODO: Implement with Vue Router
//
// This should mirror the interface of adapters/react/force-logout.ts
// using Vue Router for navigation and a Vue dialog/toast for UI feedback.
//
// Required behavior:
//   1. Clear auth state via Pinia store (logout + resetState)
//   2. Show session expired dialog or toast
//   3. Navigate to login via router.replace('/')
